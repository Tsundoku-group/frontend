'use client'

type Props = {
    params: {
        slug: string;
    }
}

const GroupPage = ({params: {slug}}: Props) => {
    return (
        <div>
            hello
        </div>
    )
}

export default GroupPage;